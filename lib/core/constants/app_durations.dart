abstract final class AppDurations {
  static const Duration paletteTransition = Duration(milliseconds: 300);
  static const Duration navIndicator = Duration(milliseconds: 280);
  static const Duration entrance = Duration(milliseconds: 400);
  static const Duration entranceStagger = Duration(milliseconds: 80);
  static const Duration snackBar = Duration(seconds: 4);
}
